# Agent Rules

- **Global Skills Location:** Always check `~/.agents/skills` for global skills and custom instructions. When searching for available skills or executing a skill, inspect `~/.agents/skills/<skill-name>/SKILL.md` for workflow rules, templates, and helper scripts.
